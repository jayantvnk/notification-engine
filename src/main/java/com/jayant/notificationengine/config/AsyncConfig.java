package com.jayant.notificationengine.config;

// TODO: @Configuration - defines the TaskExecutor used by @Async delivery,
// so ingestion returns immediately while delivery happens off-thread.
public class AsyncConfig {
}
