package com.jayant.notificationengine.delivery;

// TODO: Template Method abstract class.
// public final void send(EvaluationContext context) {
//     validate(context); format(context); doSend(context); logResult(context);
// }
// Subclasses (Email, Sse) implement the channel-specific steps.
public abstract class NotificationChannel {
}
