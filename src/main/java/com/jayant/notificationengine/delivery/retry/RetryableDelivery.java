package com.jayant.notificationengine.delivery.retry;

// TODO: wraps a channel's send() call with retry + backoff.
// Wraps AROUND a channel - not baked into each channel class.
public class RetryableDelivery {
}
