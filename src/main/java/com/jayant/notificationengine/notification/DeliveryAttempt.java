package com.jayant.notificationengine.notification;

// TODO: JPA entity - one delivery attempt on one channel for one Notification.
// Fields: id, notification (FK), channel, status (PENDING/SUCCESS/FAILED/RETRYING),
// retryCount, attemptedAt, errorMessage (nullable).
public class DeliveryAttempt {
}
