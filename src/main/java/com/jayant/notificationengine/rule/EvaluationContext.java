package com.jayant.notificationengine.rule;

// TODO: immutable value object built up as an event moves through the rule chain.
// Fields: event (NotificationEvent), channels (immutable Set<NotificationChannel>),
// priority (enum), suppressed (boolean), message (String).
// Use Lombok @Value + @Builder(toBuilder = true) for "copy with one field changed".
public class EvaluationContext {
}
